pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "My way to reinvent myself"
                sh 'df -h'
		sh 'lsblk'
                echo 'not using shell in the Jenkinsfile'
            }
        }
    }
}
